# Calculator-CLI

Simple Python command-line calculator supporting add, minus, multiply, divide, and trig functions (cos, sin, tan).

## Run

```bash
python main.py
```

Enter an operation, then space-separated numbers.
